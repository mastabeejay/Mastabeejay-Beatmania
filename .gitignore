# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Leaderboard SQLite database (runtime data, not source)
server/data

# Local env vars (Supabase URL/key) — see .env.example
.env
.env.local

# Leftover duplicates — the files actually served are the ones under public/audio/
/Hiphop_Deejaying.mp3
/Fatherslife.mp3
/We2026.mp3

# Raw source folder for the YBJ 힙합 트랙 pool — the files actually served are the copies under
# public/audio/ybj/ (renamed to safe ASCII slugs to avoid URL-encoding issues).
/YBJ_music

# Raw source folder for the main-screen BGM playlist — the files actually served are the copies
# under public/audio/mainbgm/ (renamed to safe ASCII slugs), see src/game/MainBgm.ts.
/MAIN_BGM

# Source app-icon artwork — the files actually served are the resized copies under public/icons/.
/MBBMicon.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
