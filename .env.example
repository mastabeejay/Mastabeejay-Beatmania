VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxx

# Chatbot (optional) — free-tier Gemini API key from https://aistudio.google.com/apikey.
# Restrict it to your site's domain via an HTTP referrer restriction in AI Studio before deploying.
# Without this, the chatbot still works using only the fixed FAQ fallback (see ChatbotFaq.ts).
VITE_GEMINI_API_KEY=your-gemini-api-key-here
